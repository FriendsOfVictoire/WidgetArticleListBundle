{% set url = path('victoire_core_page_show', {'url' : item.url }) %}

<tr>
    <th>
        <div>
            <span>
                {{ item.publishedAt ? item.publishedAt|date("d/m/Y") : '--' }}
            </span>
        </div>
    </th>
    <td>
        <a href="{{ url }}">
            {{ item.name }}
        </a>
    </td>
</tr>
